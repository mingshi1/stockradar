# V1.0.0 RC4.23 Android 测试

## 1. 新建并保存任务

进入：

```text
自动任务
```

点击：

```text
新建任务
```

填写：
- 任务名称
- 时间
- 板块
- 分析模式
- 报告类型

点击：

```text
保存任务
```

按钮下方应立即先出现：

```text
正在保存任务…
```

随后出现：

```text
✓ 任务 #N 已保存到本机。
当前状态：已启用。
可使用“立即执行”测试；
Android 系统级后台调度尚未启用。
```

同时“已保存任务”表格应立即看到对应任务。

Android 不应弹出：

```text
Windows Task Scheduler 注册失败
```

## 2. 修改已有任务

点选“已保存任务”中的一行，
修改名称/时间/板块后再次点击：

```text
保存任务
```

预期更新同一个任务，而不是无响应。

## 3. 任务运行期间保存

点击：

```text
立即执行
```

任务运行期间：
- “立即执行”应禁用
- “保存任务”仍应可点击
- “新建任务”仍应可点击

如果自动任务进入：

```text
联网失败
→ 等待 5 分钟自动重试
```

“保存任务”也必须仍然可用。

注意：运行中的这一次任务使用启动时的配置快照，
此时保存的新配置会用于下一次运行。

## 4. Windows 回归

Windows 版仍应：
- 保存 SQLite
- 启用任务时注册 Windows Task Scheduler
- 停用/删除时注销系统计划任务

Android 不执行上述 Windows 调度调用。
