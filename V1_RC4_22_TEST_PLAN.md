# V1.0.0 RC4.22 Android 测试

## 1. 历史报告比例

打开：

```text
历史报告
```

预期手机布局：

```text
历史报告

[ 历史记录列表：约 150~210px 高 ]

[ 完整报告预览：纵向大区域 ]
```

不应再出现：
- 左边列表占绝大部分宽度
- 右边报告只有一条窄竖栏

列表与报告均支持手指滚动。

## 2. 自动任务页面

已保存任务手机表格只显示：

```text
名称 | 时间 | 状态 | 最近结果
```

最近自动运行记录只显示：

```text
时间 | 任务 | 结果
```

不再强迫用户横向拖动整张桌面表格。

## 3. 自动任务联网失败 5 分钟重试

自动任务执行中，如果首次遇到网络类错误，例如：

```text
IncompleteRead
RemoteDisconnected
ConnectionResetError
timeout
HTTP 500 / 502 / 503 / 504
```

任务状态应变成类似：

```text
自动任务遇到联网失败，
将在约 5 分钟后自动重试 1 次。
预计重试时间：HH:MM:SS
```

5 分钟后自动再执行一次分析。

### 不自动重试

以下错误直接失败：
- HTTP 400 / 401 / 402 / 403 / 404 / 409 / 422 / 429
- Authentication / API Key 无效
- TLS certificate verify failed
- 余额不足 / quota / rate limit
- 其它配置错误

### 重试次数

```text
首次请求
→ 网络失败
→ 等待 5 分钟
→ 自动重试一次
→ 成功或最终失败
```

不会无限重试。

## 4. 注意

Android Beta 目前仍不是系统级可靠后台服务。
如果等待重试的 5 分钟期间系统强制杀掉 App 进程，
这次内存中的重试也会结束；Windows `--run-task`
则会保持进程直到重试完成。
